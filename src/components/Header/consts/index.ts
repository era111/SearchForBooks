export enum Categories {
  All = "all",
  Art = "art",
  Biography = "biography",
  Computers = "computers",
  History = "history",
  Medical = "medical",
  Poetry = "poetry",
}

export enum Sorts {
  Relevance = "relevance",
  Newest = "newest",
}

export const MAX_RESULT = 30